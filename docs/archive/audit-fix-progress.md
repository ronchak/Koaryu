# Audit Fix Progress

Source audit: `in-depth-codebase-review-findings.md`

Started: 2026-05-23

Archive note: this ledger is historical evidence from the remediation pass that started on May 23, 2026. It is not a current correctness certificate for the active worktree. Treat each `Fixed`, `Closed no-op`, `GREEN LIGHT`, and section-review entry as a record of what was verified at the time it was written. Before citing any row as current proof, re-inspect the current source and rerun the relevant checks for the files, database state, and environment being released.

Starting worktree note: `git status --short` showed many pre-existing modified and untracked files. Treat all existing changes as user work unless a later note in this ledger says otherwise.

Historical status key:

- `Not started`: not yet inspected for this fix pass.
- `In progress`: current-code verification or implementation was underway when the row was last updated.
- `Fixed`: code changed and reviewer approved with `GREEN LIGHT` during that pass.
- `Closed no-op`: code evidence showed no change needed and reviewer approved with `GREEN LIGHT` during that pass.
- `Blocked`: cannot safely proceed without external credentials, production state, or product judgment.

## Frontend Findings

| ID | Finding | Status | Notes |
| --- | --- | --- | --- |
| F01 | Reports utilization can be inflated by mixing capacity-tracked and non-capacity attendance | Fixed | Added `report-metrics` helper/tests; overall and program utilization now use capacity-tracked attendance only. Targeted lint/test passed. Reviewer GREEN LIGHT from Archimedes. |
| F02 | Billing page can show a loading state after a failed live billing load | Fixed | Added billing loading-state helper/tests; failed settled loads no longer show spinner, with unknown-role loading preserved. Targeted lint/test passed. Reviewer GREEN LIGHT from Arendt. |
| F03 | Student row checkbox can trigger two selection toggles from one click path | Fixed | Checkbox click now stops propagation before its own `onChange` toggles selection. Targeted lint/test passed. Reviewer GREEN LIGHT from Erdos. |
| F04 | Some date logic uses UTC-derived date keys in UI areas that otherwise use local date keys | Fixed | Reports, student detail hold logic, and student insights now use local date helpers; added UTC-safe calendar day diff and timezone/DST tests. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Anscombe. |
| F05 | Dashboard performs a second billing summary fetch when backend summary billing is absent | Fixed | Removed dashboard fallback Billing endpoint orchestration; dashboard now relies on compact backend `summary.billing` with preview/null fallbacks. Targeted frontend lint/test/typecheck and backend summary tests passed. Reviewer GREEN LIGHT from Dirac. |
| F06 | The API proxy target URL is assembled from raw path segments | Fixed | Added encoded/rejecting proxy target helper; proxy returns private JSON 400 for unsafe dot/slash segments. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Aquinas. |
| F07 | Auth callback accepts arbitrary same-origin `next` paths | Fixed | Added auth callback next-path allowlist for `/dashboard`, `/onboarding`, and `/reset-password`; rejects arbitrary same-origin/API paths and non-slash raw values. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Kant. |
| F08 | Sitemap freshness is generated from the current clock for every route | Fixed | Replaced request-clock sitemap freshness with stable public content timestamp and regression test. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Schrodinger. |
| F09 | Auth pages are not explicitly excluded from indexing in the auth layout or robots file | Fixed | Added auth-layout noindex/nofollow metadata and robots disallows for `/login`, `/signup`, `/reset-password`. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Plato. |
| F10 | Landing page header/footer markup duplicates public-navigation concepts found elsewhere | Fixed | Added shared public navigation/footer constants and reused them in landing, marketing shell, and mobile nav defaults. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Hooke. |
| F11 | Global CSS is carrying many unrelated application concerns | Fixed | Moved loading-screen, account-menu motion, landing hero preview, and shared public marketing visual selectors out of `globals.css` into CSS modules; removed unused marketing selectors after source search. Added source-scope regression tests. Targeted lint/tests/typecheck/diff-check passed. Reviewer GREEN LIGHT from Cicero. |
| F12 | Marketing detail routes repeat the same route structure | Fixed | Added a shared marketing detail route helper for static params, metadata, breadcrumbs, structured data, related-page mapping, and render wiring; route files now hold only per-route config. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT from Carson. |
| F13 | Modal behavior is implemented separately in several components, with inconsistent accessibility details | Fixed | Added shared `ModalFrame`, migrated direct modal skeleton call sites, and required consistent dialog labelling including StudentForm. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT from Gibbs. |
| F14 | Reusable form fields do not consistently connect error/hint text to controls | Fixed | Added ARIA/id wiring for reusable `Input`, `ProgramPicker`, class modal `SelectField`, and StudentForm custom status/notes controls. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT from Mendel. |
| F15 | `AccountMenu` combines menu mechanics, billing state, theme controls, and layout logic | Fixed | Extracted menu open/close/position/focus mechanics into `useAccountMenuController`, billing snapshot fetching into `useAccountMenuBillingStatus`, and viewport math into a pure helper. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT from Boyle. |
| F16 | `StudentForm` keeps a large amount of form state in one component | Fixed | Moved StudentForm field state, validation, tab/error handling, and `StudentCreate` payload construction into `student-form-state.ts`; component now renders from the hook. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT from Lovelace. |
| F17 | Marketing reusable components include page content strategy and faux product visuals | Fixed | Moved faux product visualization into `product-scene.tsx` and default public-page content strategy into `marketing-public-content.ts`, preserving public APIs. Targeted lint/test/typecheck/diff-check passed. Reviewer GREEN LIGHT. |
| F18 | Some exported UI components appear unused in the searched source tree | Fixed | Removed unused `ThemeToggle` file and `SmallStatusDot` export, with source guard test. Targeted lint/test/typecheck/diff-check passed. Read-only reviewer GREEN LIGHT. |
| F19 | Frontend tests do not match the documented Node 18 baseline | Fixed | Aligned README, frontend README, package engines, lockfile metadata, and frontend agent guidance on Node.js 22.13+ for the type-stripping test runner. Added runtime-baseline guard test. Targeted lint/test/full frontend test/diff-check passed. Read-only reviewer GREEN LIGHT. |
| F20 | The Playwright E2E test is live-stateful and logs account identity values | Fixed | Playwright belt-ladder spec is now skipped by default unless `KOARYU_LIVE_STATEFUL_E2E=true`, uses explicit `KOARYU_E2E_*` live-test inputs, requires a caller-provided disposable studio name, and no longer logs account identifiers. Added safety guard test and docs. Targeted lint/test/typecheck/skip-check/diff-check passed. Read-only reviewer GREEN LIGHT. |
| F21 | Preview CSV import maps program and belt names directly into id fields | Fixed | Preview import now resolves program/rank display values through `resolvePreviewImportStudentIds` before assigning ids, emits unresolved row warnings before row validity recording, and avoids cross-program belt assignment when a supplied program is unknown. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Linnaeus. |
| F22 | Performance marks use shared names for potentially overlapping operations | Fixed | Added request-scoped `startPerformanceSpan` marks and switched student page fetch instrumentation off shared `students.page_started` / `students.page_finished` names. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Darwin. |
| F23 | Import idempotency key generation uses custom hashing and only canonicalizes part of the input | Fixed | Import keys now canonicalize nested request input and use Web Crypto SHA-256; the import page prepares the async key in state before enabling import. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Ohm. |
| F24 | The frontend API wrapper drops falsy request bodies | Fixed | Added explicit JSON body serialization that omits only `undefined`, preserving `null`, `false`, `0`, and empty-string bodies. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Turing. |
| F25 | Frontend state and type definitions have become unusually large central files | Fixed | Extracted preview student paging out of `store.tsx` while reusing the existing `StudentListResponse` contract, moved dashboard summary contracts out of the central type barrel, and moved billing/dashboard route utilities into focused lib modules. Targeted lint/test/typecheck passed. Reviewer GREEN LIGHT from Avicenna. |

## Backend Findings

| ID | Finding | Status | Notes |
| --- | --- | --- | --- |
| B01 | Disabling autopay only updates Koaryu local payer state in the reviewed method | Fixed | `disable_autopay` now rewires active payer autopay subscriptions to Stripe invoice collection before local payer disable, updates local subscription/enrollment collection mode, and records rewired subscription ids. Targeted billing lifecycle test passed. Reviewer GREEN LIGHT from Dalton. |
| B02 | Refund and dispute projection updates payment/refund/dispute rows, not invoice or payer balance rows in the inspected block | Fixed | Refund/dispute projection now refreshes affected invoice paid/remaining/status from payment rows and recomputes payer balance, including late-linked disputes and full-refund consistency. Targeted billing lifecycle test passed. Reviewer GREEN LIGHT from Curie. |
| B03 | Webhook claiming reads before updating state and does not use a unique processing token in the inspected path | Fixed | Stripe webhook rows now claim with a UUID `processing_token`, compare-and-swap status/token filters, `processing_started_at` stale detection, and token-guarded completion/failure updates. Added migration and targeted tests. `test_webhook_service.py`, Supabase lint, and diff check passed. Reviewer GREEN LIGHT from Mill. |
| B04 | Account deletion processing selects scheduled rows before deleting users and marking completion | Fixed | Account deletion processing now claims due rows with a private processing token and lease timestamp before Auth deletion, skips fresh claims, recovers stale claims, and token-guards completion/cancel writes. Added migration and targeted tests. `test_account_service.py`, Supabase lint, and diff check passed. Reviewer GREEN LIGHT from Russell. |
| B05 | Connect onboarding link creation passes caller-supplied redirect URLs without using the local safe-redirect helper | Fixed | Connect onboarding now validates `refresh_url` and `return_url` through `_safe_redirect_url` before Stripe link creation or first-time account creation. Targeted billing lifecycle test and diff check passed. Reviewer GREEN LIGHT from Peirce. |
| B06 | Billing system status labels a read query as a write-path check | Fixed | Billing system status now labels the `studio_payment_accounts` select as `Supabase billing read`, with a regression assertion that the old write-path label is absent. Targeted billing lifecycle tests and diff checks passed. Reviewer GREEN LIGHT from Euclid. |
| B07 | Report export row fetches do not show pagination in the inspected methods | Fixed | Report export table and intelligence dataset fetches now use ranged Supabase pagination with deterministic ordering, and direct `student_guardians` relationship fetches are chunked/paged. Targeted report export tests and diff checks passed. Reviewer GREEN LIGHT from Fermat. |
| B08 | Student import claiming and execution are application-level multi-step processes | Fixed | Student import runs now use UUID processing tokens, lease timestamps, token/lease compare-and-swap reclaiming, heartbeat renewal during execution, and token-guarded completion/failure. Added migration and targeted claim race tests. Import tests, Supabase lint, and diff checks passed. Reviewer GREEN LIGHT from Goodall. |
| B09 | Lead conversion performs multiple writes and skips membership creation when the deterministic student already exists | Fixed | Lead conversion now always runs an idempotent membership ensure after deterministic student creation/check, repairing retries where the student exists but membership is missing. Targeted lead service test and diff check passed. Reviewer GREEN LIGHT from Franklin. |
| B10 | Promotion recording inserts promotion history before separately updating current rank fields | Fixed | Promotion finalization now runs through backend-only `record_student_promotion` RPC, which records promotion, updates membership/student rank fields, and writes audit in one database function call. Targeted belt test, Supabase lint, and diff check passed. Reviewer GREEN LIGHT from Zeno. |
| B11 | Some read-oriented paths perform repair writes | Fixed | Program and ladder list paths no longer call `ensure_program_ladders`; explicit write/import repair paths remain. Targeted program/belt tests and diff checks passed. Reviewer GREEN LIGHT from Aristotle. |
| B12 | Recurring schedule materialization reads existing sessions, builds missing rows, and inserts them | Fixed | Verified materialization still performed a read/derive/plain insert sequence. Added duplicate-conflict fallback that skips only conflicting generated sessions and keeps inserting remaining rows. Targeted pytest passed; Locke reviewer GREEN LIGHT. |
| B13 | Staff invitation creates or invites the auth user before inserting the Koaryu staff role | Fixed | Verified `invite_staff` called Supabase Auth before membership persistence. Added pending staff-role reservations before Auth invite, durable claim-on-login by exact normalized Auth email for post-invite link failures, null-user pending schema support, frontend/backend type handling, and targeted tests. Pascal reviewer GREEN LIGHT. |
| B14 | Billing payer response aliases payment method type to card brand | Fixed | Verified `BillingPayerResponse` copied card brand into `stripe_payment_method_type`. Added method-type normalization that returns `card` for card evidence, preserves explicit method types, and keeps non-card fallback behavior. Targeted schema and billing lifecycle tests passed; Tesla reviewer GREEN LIGHT. |
| B15 | Schedule schemas compare date/time values as raw strings | Fixed | Verified schedule schemas compared raw strings. Added explicit `HH:MM`/`YYYY-MM-DD` shape validation and parsed date/time comparisons, including Python-version-safe date regressions. Targeted schedule schema/service tests passed; Laplace reviewer GREEN LIGHT. |
| B16 | Auth token fallback can expose provider/config detail in the response body | Fixed | Verified invalid-token fallback response interpolated JWT/Supabase exception strings. Replaced fallback failure detail with a generic 401 while preserving expired-token and successful fallback behavior. Targeted security tests passed; Ramanujan reviewer GREEN LIGHT. |
| B17 | Missing credentials and invalid credentials use different default status paths | Fixed | Verified default `HTTPBearer()` handled missing auth outside app code. Switched to `auto_error=False` and made `get_current_user_id` return the same explicit 401 shape for missing/empty credentials as invalid tokens. Targeted auth dependency/security tests passed; Ptolemy reviewer GREEN LIGHT. |
| B18 | Some backend request schemas are broad relative to endpoint-specific needs | Fixed | Verified broad billing action/enrollment request schemas. Replaced billing action body with endpoint-specific request models, tightened enrollment create schemas by route shape, preserved aliases, and added schema coverage. Billing schema/lifecycle tests passed; Einstein reviewer GREEN LIGHT. |
| B19 | Platform subscription access depends on local projected subscription rows | Fixed | Verified access checks read local projected subscription rows. Added an access-status repair path that refreshes stale stored subscription ids and expired local trialing rows from Stripe before access decisions, with local fallback on repair failure. Targeted platform/access tests passed; Herschel reviewer GREEN LIGHT. |
| B20 | Demo reset clears and reseeds many tables through sequential service operations | Fixed | Verified broad clear/update/seed sequence. Added explicit reset phases, seed-only compensating cleanup, sanitized failure audit metadata, response factoring, and orchestration tests for seed/clear/update/audit failure behavior. Targeted demo tests passed; Newton reviewer GREEN LIGHT. |

## Backend Test Findings

| ID | Finding | Status | Notes |
| --- | --- | --- | --- |
| T01 | Billing/webhook tests use custom fake Supabase and Stripe layers | Fixed | Verified custom fake Supabase/Stripe layers remain in billing/webhook service tests. Added FastAPI endpoint contract tests for billing onboarding and Stripe connect webhook boundaries using TestClient and patched services so the public entry points are not covered only by bespoke fake layers. Targeted tests passed; Rawls reviewer GREEN LIGHT. |
| T02 | Billing idempotency tests call private helpers directly | Fixed | Verified direct private-helper/object-construction patterns in billing, webhook, and platform billing tests. Moved the audited cases onto public service handlers/normal constructors and confirmed the named private-helper patterns no longer match. Targeted tests passed; Galileo reviewer GREEN LIGHT. |
| T03 | Class-level fake Stripe state is mutated across tests | Fixed | Verified class-level fake Stripe call/response state and singleton account persisted across tests. Added reset helpers for `_FakeStripe` and `_FakeStripeService` and reset them in `BillingPaymentsLifecycleTest.setUp()`. Targeted lifecycle tests passed; Harvey reviewer GREEN LIGHT. |
| T04 | Fake Supabase implementations are duplicated across many test files | Fixed | Verified duplicate fake Supabase/table/query/result layers across the named test files. Added shared `tests.fakes.supabase` helpers and migrated 7 of 8 named files to the shared table-backed fake, leaving only the student-list RPC/search/range fake specialized. Targeted tests passed; Singer reviewer GREEN LIGHT. |
| T05 | CSV fixtures are PII-shaped and only loosely asserted in the broad fixture test | Fixed | Verified broad fixture loop only asserted counts and one mapping exclusion. Added fixture-specific expected mapping/skipped-column checks and representative import-plan assertions for PII-shaped CSV fixtures, including billing exclusions and sensitive field placement. Targeted CSV import tests passed; Bacon reviewer GREEN LIGHT. |

## Supabase Findings

| ID | Finding | Status | Notes |
| --- | --- | --- | --- |
| S01 | Support triage digest emits raw truncated subject/details for non-student-record topics | Fixed | Added forward migration to make digest subject/summary metadata-only for non-student topics, expanded support smoke privacy assertions, and updated runbook language. Linked lint and transactional linked support/account smoke passed; Noether reviewer GREEN LIGHT. |
| S02 | Direct-client write RLS is broad on core operational tables | Fixed | Added migration to drop direct browser write policies on students/programs/student_program_memberships, revoke anon/authenticated write grants, and preserve service_role CRUD; added verification SQL. Linked lint and transactional linked verification passed; Averroes reviewer GREEN LIGHT. |
| S03 | Belt-ladder sync function history shows repeated repair churn | Fixed | Preserved migration history, documented the final RPC audit entrypoint in Supabase guidance and smoke comments, and strengthened the smoke with a tenant-boundary negative check. Linked lint/account-support/belt smoke passed; Pauli reviewer GREEN LIGHT. |
| S04 | Program-filter RPC returns total count through a left join that can produce a null-student sentinel row | Fixed | Added owned verification SQL for the null-student sentinel contract and backend regression coverage for preserving totals while skipping null-id fetches. Linked lint, transactional linked RPC verification, and targeted backend test passed; Halley reviewer GREEN LIGHT. |

## Docs, Scripts, and Repo Hygiene Findings

| ID | Finding | Status | Notes |
| --- | --- | --- | --- |
| D01 | Stripe Connect smoke script can operate against whichever environment variables are loaded | Fixed | Added explicit stateful-target confirmation, account/newest-account, and remote-endpoint guardrails before env loading/posting; updated Render runbook. Safe syntax/help/failure checks passed; Poincare reviewer GREEN LIGHT. |
| D02 | Dev scripts kill processes occupying ports 4000 and 8001 | Fixed | Reworked dev scripts to stop only recorded Koaryu dev PIDs whose start time, cwd, and command match, and to refuse untracked port owners. Shell and isolated process-safety checks passed; Sagan reviewer GREEN LIGHT. |
| D03 | README verification snapshot is broad and undated | Fixed | Reframed README verification snapshot as historical, dated the archive note, and pointed current evidence to the active remediation ledger; Lagrange reviewer GREEN LIGHT. |
| D04 | Pricing/commercial positioning differs across docs | Fixed | Clarified `$27/month` as current public/outreach positioning, `$49/month` as competitor context, and PRD tier table as archived strategy; Pasteur reviewer GREEN LIGHT. |
| D05 | Root `tmp_import_tests/` contains PII-shaped CSV fixtures and is untracked/unignored | Fixed | Added git/Vercel ignores for the local scratch folder and a local-only README note without deleting user scratch files or exposing row data; Helmholtz reviewer GREEN LIGHT. |
| D06 | PRD citation quality has visible provenance issues | Fixed | Removed visibly mismatched `[^24]` markers from PRD Stripe/stack/security claims and added archived-citation caveat; Socrates reviewer GREEN LIGHT. |
| D07 | Render plan language differs between config and runbook | Fixed | Aligned Render runbook and README demo guidance with the configured `starter` service/small-instance behavior; McClintock reviewer GREEN LIGHT. |

## Section Reviews

| Section | Status | Reviewer | Notes |
| --- | --- | --- | --- |
| Frontend F01-F25 | Fixed | Mencius | Section-final reviewer inspected frontend audit coverage, ledger, staged/unstaged/untracked diffs, high-risk paths, and safe checks; GREEN LIGHT. |
| Backend B01-B20 | Fixed | Sartre | Section-final reviewer inspected audit coverage, backend status/diffs, relevant untracked tests/migrations, representative backend paths, and safe checks; GREEN LIGHT. Local Supabase lint remained blocked by local DB refusal, but linked lint passed. |
| Backend tests T01-T05 | Fixed | Parfit | Section-final reviewer inspected audit coverage, ledger, backend test changes including untracked files, and safe checks; GREEN LIGHT. |
| Supabase S01-S04 | Fixed | James | Section-final reviewer inspected S01-S04 audit coverage, tracked/untracked Supabase/backend files, section diffs, and safe checks; GREEN LIGHT. |
| Docs/scripts/repo hygiene D01-D07 | Fixed | Godel, Planck | Section-final reviewer inspected D01-D07 audit coverage, ledger, actual docs/scripts diffs, ignored scratch-data handling, and safe checks; GREEN LIGHT. Planck re-reviewed the later Supabase helper `--file` fix and linked-state drift classification; GREEN LIGHT. |
| Whole-system final review | Fixed | Epicurus | Final reviewer checked the ledger against all 61 audit findings, cross-system contracts, billing/data consistency, security/privacy, docs/scripts guardrails, and safe checks; GREEN LIGHT. Linked Supabase migration drift remains a release/deploy caveat, not an unresolved code defect. |
