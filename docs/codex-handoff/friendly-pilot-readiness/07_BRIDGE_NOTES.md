# Bridge Notes

## Owner intent

- Shipping a reliable single-studio pilot matters more than preserving multi-studio as an active feature.
- Preserve latent multi-studio infrastructure only when safer than deleting it.
- Front Desk should remain operationally powerful.
- Instructor should remain operationally useful but financially isolated.
- Instructor billing denial is a security boundary, not a hidden-button preference.
- Complete tuition lifecycle control remains a long-term goal.
- Do not turn that roadmap into an oversized release.
- Mobile usability and documentation are part of pilot quality.
- Production demo data remains.
- Email is the preferred operational alert channel.
- Provider-native recovery and alerts are preferred over custom frameworks.
- The physical recovery-key copy remains a human task.

## Billing bridge

The previous combined prompt was independently reviewed.

The reviewer found the original billing scope unsafe because it listed the entire tuition lifecycle while also requiring one bounded release.

The corrected plan requires an early disposition:

- CONTRACT ONLY;
- NARROW TEST-MODE SLICE;
- SEPARATE BILLING RELEASE.

Default to CONTRACT ONLY unless current code proves that a narrow test-mode slice is already substantially complete.

Do not treat the billing roadmap as release acceptance criteria.

## Review-loop bridge

The improved loop adds:

- durable checkpointing;
- interruption recovery;
- SHA-indexed evidence;
- explicit agent shutdown;
- one implementation stream;
- bounded domain reviewers;
- one correction pass per domain;
- one final correction pass;
- candidate freeze and evidence invalidation;
- separate application, migration, and live-Stripe approvals.

These are deliberate safeguards for a long-running Sol Ultra session.

Do not simplify them away after context compaction.

## Remaining human decisions

Do not ask for these until concrete implementation evidence makes them necessary:

- exact Stripe pause semantics;
- billing-anchor rules;
- immediate proration behavior;
- trial conversion behavior;
- refund accounting behavior;
- live transition activation;
- application production deployment;
- any production migration;
- physical flash-drive completion.

When a financial decision is needed, present concrete options with a recommended safe default.

## Next safe action

1. Read every attachment completely.
2. Read the repository root AGENTS.md.
3. Create a fresh worktree from verified remote main.
4. Initialize the durable checkpoint.
5. Run Phase 0 read-only reconciliation.
6. Publish the classified ledger.
7. Lock Friendly Pilot Core and billing disposition.
8. Only then begin implementation.

## Scope warning

If the billing work threatens the core release:

- do not increase the line/file budget;
- do not add more agents;
- do not weaken testing;
- do not skip review;
- choose CONTRACT ONLY or SEPARATE BILLING RELEASE.

The release is successful when the pilot core is reliable and billing behavior is honest, even if broad live tuition control remains deferred.
