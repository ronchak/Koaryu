# Release attestation generator verification

This change removes repeated release authoring and the V31 restore program splice. It adds no migration and changes no persisted business behavior. All 136 historical migrations, including V41, remain unchanged.

## Generated coverage

The generator reproduces eleven complete historical SQL statements: V39/V40/V41 full preflights and compatibility adapters, three rank-return manifests, and two rank-receipt manifests. Fixed source digests and comparison with committed statements protect the original bytes. Older attestation families remain immutable inputs; this is not a claim that all 105 inventoried historical statements were regenerated.

All ten existing restore scripts reproduce their complete committed bytes and file modes. The current-input comparison is separate from reproduction against the pinned historical Git baseline, so a future inventory suffix cannot silently rewrite historical proof. Explicit business fixtures retain the financial, membership, rank, replay and concurrency assertions that schema alone cannot generate.

The additional generated V31-through-V37 script runs the V31 proof once and applies explicit subsequent stages. It checks the same V34–V37 catalogs, V35–V37 readiness and V37 trigger guard as the former splice. Its new local-target guard uses the existing PostgreSQL verifier and clears inherited PG routing variables before any mutation.

## Shared state and application contracts

One declared identity table supplies heads, counts, pending lists and preflight identities. Generated Node aliases and backend metadata use those facts. The two V25 states remain distinct. Older restore cases become version-bound when a later declared release replaces the moving FINAL alias.

An export-by-export comparison found exactly nine intended changes to existing rollout constants: historical pending lists for counts 100–108 now end at their own heads. Their other tuple fields are unchanged. All other existing exported constants retain their prior values. The complete candidate packet was identical before and after consolidating history calculations. Explicit raw-evidence acceptance and authorization decisions remain in the guarded rollout tool.

A declaration-only synthetic next release exercises SQL and Python generation, current backend metadata, older V41 binding, canonical/restored execution structure and Python syntax. The test denies access to original repository targets. A readiness-only forward case is rejected because it lacks explicit canonical/restored evidence. This is generation and syntax proof; it is not execution of a future business migration.

## Verification

- Offline generation checks compare historical SQL, all ten historical scripts, their modes, the new continuation and generated application metadata.
- Output generation under a restrictive umask preserves all eleven script modes and bytes. A nonempty output directory is refused.
- A hosted-target negative proves the new continuation exits before invoking any database tool.
- The complete disposable PostgreSQL 17 runner passed all 136 migrations and 51 contracts, historical restores and existing concurrency checks with the new combined continuation.
- Focused backend readiness and restore-guard tests passed 17 cases after adopting generated backend constants.
- Rollout, workflow and inventory checks passed. The existing rollout suite consolidates 68 cases into 66 while retaining its negative contracts; generator-specific checks protect the newly introduced authoring contract.

No test was added merely to require a private implementation name or a version string in prose. Exact generated bytes are a required contract here. Source-text test cleanup elsewhere remains pending.

The coordinator reviewed the Python renderer from a separate Astra task, required a reusable forward profile, corrected moving-alias handling, and added explicit canonical/restored evidence requirements. A separate fresh PR reviewer and final exact-head CI are still required before merge.

No provider state, production database, production deployment, historical financial data or approval was changed. Generated output does not grant release authority. Future schema changes still need reviewed pins, meaningful business fixtures, the full database proof and the existing human-only production gates.
