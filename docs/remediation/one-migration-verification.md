# One-migration verification

Code candidate: `3656a3b7988ba8e3dc0db7f1d25334338b73cb4a`, based on PR208 main `ea41d8604e255de9869b05c5e84e4363d2d432b9`. Later documentation commits retain identical tool and migration bytes. Fresh independent review and exact-head CI still apply to the final PR head.

The coordinator exercised nine separate invocations through the tool's real source verifier, filesystem scoping and remote-state reader, using Supabase CLI 2.95.4 against disposable PostgreSQL 17. The CLI used a reserved localhost TLS port; state queries used the owned Unix socket. Only hosted linking and the GitHub approval response were substituted. Every invocation performed a full-remainder dry-run, a singleton dry-run and one actual CLI apply, then passed the declared history, raw-catalog and readiness checkpoint. Final state was exact V47, 142 migrations. All historical source bytes remained unchanged. The test server, certificate and checkout scopes were removed.

The initial contract-verifier bootstrap was not a faithful CLI-history fixture: it made `statements` non-null with a default and retained `.sql` in migration names. The guarded state reader refused it. The coordinator corrected only that disposable setup to match the pinned CLI. The CLI's connection handling also required loopback TLS for this proof. No production validation or transport guard was relaxed.

Private reproducible proof and results are under `.git/orchestrator/20260914-refund-release/`: `probe-one-migration.sh`, `one-migration-local-proof.mjs`, `one-migration-local-proof-5.log`, and `one-migration-local-evidence.json`. These contain synthetic test evidence, not a production backup or hosted rehearsal.

The existing unit contracts were extended for invalid mode/token/approval/confirmation and wrong successor behavior. Three behavioral cases add the nine declared selectors, real filesystem restoration including partial move failure, and a main-entry single-apply workflow. An earlier applied file remains visible while later files are parked. Case count is 67→70; workflow checks are 128→131. Tool source is 5,612→5,736 lines; tests are 3,702→3,922 lines. The movement of the existing apply body accounts for necessary indentation changes. No source-text test or fixture framework was added.

The CLI determines its pending list from the visible ordered migration files and applies each file with its history entry in one batch. See the pinned [pending-file implementation](https://github.com/supabase/cli/blob/v2.95.4/pkg/migration/apply.go) and [transaction implementation](https://github.com/supabase/cli/blob/v2.95.4/pkg/migration/file.go). The actual local CLI run verifies those behaviors for this chain. Hosted staging, provider-specific restore evidence and production remain separate gates.
