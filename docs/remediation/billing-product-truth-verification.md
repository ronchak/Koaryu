# Billing product-truth verification

Base `04d7efe39e10cb9e05d60779a2114deff0b6375b`; implementation `859a5a8772bc5271071c15cb1b4d33b3056f5f28`. Fixes CTA1-01, BB2-10, DOC1-01, FT1-05 and FR1-09.

The boundary now links to the existing workflow catalog instead of repeating obsolete capability, control and endpoint inventories. README and customer copy distinguish Core subscriptions from tuition activation. No studio grant or general tuition availability is asserted. Runtime permissions, providers, money, generated contracts and database files are unchanged.

The coordinator compared frontend syntax trees: all four source files differ only in copy. Retained Core entitlement, local transition contracts and approval-history sections match the base byte for byte. No unrelated Markdown reflow remains. Ten changed files total 2,542→2,361 lines and 132,713→117,589 bytes. The boundary alone shrinks 351→227 lines.

The four affected test files shrink 549→495 lines and 23,654→21,402 bytes, with 19→17 cases. Deleted cases locked obsolete provider copy and source-shaped workflow wiring. Billing role denial, exact capabilities and preview behavior remain unchanged. The existing public-price case checks imported billing content for conditional availability rather than a full source phrase. Unrelated metadata/layout and legacy-composition static checks remain for their separately tracked owner; this change does not claim to finish the source-test cleanup. Price derivation and forbidden-dependency checks remain intact.

Coordinator: 29 focused marketing, role and billing-policy cases passed. Implementer: 30 focused cases, all 909 frontend cases, targeted lint/format and a build using only synthetic .env.example values passed. The final correction restored only Markdown formatting, so the code verification still applies. Fresh independent review and exact-head CI remain mandatory before merge. No provider, authentication, migration or deployment action ran.
