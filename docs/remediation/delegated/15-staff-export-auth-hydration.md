# Selected staff Auth hydration
Findings OPS1-09 and BT5-05. Only start after root supplies an approved worktree and budget checkpoint. Read root/backend AGENTS and direct relevant code. No program history/ledger.

Own backend/app/services/report_export_data.py::_fetch_auth_users; tests/test_report_export_data_budget.py staff Auth cases, tests/test_staff_archive_contract.py export fixture/case, tests/test_report_export_service.py staff Auth fixture/assertions. No other production files, SQL/migrations, endpoint authorization, monetary code or dependencies. Root writes public recipe and ledger.

Replace global Auth pagination inside the adapter with get_user_by_id for the already distinct selected IDs derived from studio-scoped nonarchived staff roles. Delete list-response normalization, paging and catch-all suppression. Each lookup must be admitted through the existing shared provider budget, and successful returned users consume one fetched row. Keep elapsed checks and all existing limits. An AuthApiError with the confirmed user_not_found code may omit hydration because deletion can occur after role selection; other provider failures propagate and abort the export. Do not treat arbitrary 404/proxy errors, permissions, malformed responses or network errors as missing users. The pinned gotrue UserResponse requires user: User, so access response.user directly without speculative dict/getattr/None fallbacks. No call-site filter, new adapter layer, concurrency or retries.

Use actual pinned SDK types/errors in fixture contracts where helpful. The official get_user_by_id docs and local gotrue implementation were verified. Do not use StaffService._get_auth_user: its broad exception suppression would preserve the defect. Preserve role/profile merging and invited-email fallback for confirmed missing users, selected studio, archive filtering, endpoint roles, audit and budget behavior.

Strengthen the existing archive export case to observe selected-ID Auth calls, email and sign-in hydration, with legal names from staff_profiles. Seed another studio and archived staff as exclusions; preserve existing assertions. Make the Auth fixture match get_user_by_id, not a global listing. Reuse existing fixtures, no framework. Update StaffExportAuthAdmin in service tests so missing IDs raise the real user_not_found error rather than returning a response impossible under the SDK contract. Preserve all other role/profile/export expectations. The CSV deliberately has no cosmetic full_name field; keep Auth user_metadata names excluded and preserve legal-name ownership in staff_profiles. Do not add a name column or change the export contract to satisfy the shorthand name-hydration request.

Replace the old budget test that requires global paging and treats page failure as success with selected-ID hydration/call-accounting behavior amid unrelated Auth users. Cover duplicate selected IDs through the existing distinct-ID selection, if needed in this same case. Add one separately named provider-failure case proving failure propagates with no partial CSV; include a real non-missing AuthApiError and a network/runtime failure as subtests if cheap. Keep confirmed missing-user behavior distinct from provider failure.

Reduce duplicated archive-export coverage only by consolidating its original filter assertions into the strengthened archive suite; no bulk deletion of safety assertions. Preserve the deterministic export clocks from current main and all monetary, authorization, tenant and idempotency assertions. No source-text wiring checks, generic helper, unrelated reflow or extra feature.

Run affected report-export/service/budget/archive files, related endpoint/contract checks and full backend pytest because this adapter is shared. Ruff targeted formatting, diff check, generated API check unchanged. Report before/after lines and collected case counts, exact selected-ID trace/budget and failure evidence; no claim based on a fixture silently missing the called method. No commit/push/merge/subagents/docs or hosted activity. Return COMPLETE or MISMATCH before exceeding scope.
