# Roster presentation

PR176 addresses FC3-02, FC3-03 and FC3-05. Mobile sorting uses the existing controller. Student and belt badges share the existing luminance rule, fixing light-rank contrast without changing ranks or promotion behavior. Pointer entry no longer changes quick-view state while its rail is hidden; keyboard focus, selection and row opening keep their existing handlers.

The first worker patch added source-text wiring tests. Root rejected those additions. The corrected tests use the existing browser packer to mount the production roster page and both badge components. A fresh independent reviewer then caught an ineffective CSS-module export: default-imported classes were undefined, so merely injecting the real stylesheet did not prove responsiveness. The export shape is corrected. The existing browser case now verifies sort/rail/table-header visibility at 820px and 1400px, along with sorting callbacks, pointer versus focus behavior, selection, opening and yellow tip rendering. The reviewer independently passed all fourteen cases in the two touched suites after the correction and found no remaining material issue.

Seven obsolete source assertions are removed from the existing contract file, which shrinks by eight lines. The new test file has one hundred ten lines and two cases; the shared helper grows by thirteen lines. Net test/helper growth is one hundred fifteen lines. This is stronger behavioral coverage, not a claimed net test reduction. The mixed contract file retains unrelated assertions and the broad FT1-11 cleanup remains pending. The two badge components shrink by twenty-two lines, with the shared twenty-seven-line color helper replacing their separate color decisions.

The worker passed 918 full frontend tests before the final fixture and review corrections, then all 27 focused cases, TypeScript and targeted lint after the final corrections. The supported production build passed with example environment values and worktree-local locked dependencies. An earlier build could not follow the external dependency symlink; an unsupported Webpack retry was abandoned without application changes. Root repeated the fourteen touched-suite tests after rebase, all passing. Final-head release CI remains required before guarded merge.

Original reviewed head `8e802fd86753c65070592abe2ff71d144a6697b1` rebased onto PR175 main `a2f0057dfd6bb0c74a14da77aae5528d9bf2c2b3` as `a1bca5a5e5171f4ae4c44e29a18698537317e69b`. The complete implementation diff is byte-identical, SHA256 `0e4a38b132b715907e25bd6f0e5f44ec6429e790734b2c5678ca9834cb422b6c`.

The browser fixture includes actual roster CSS and component styles, but not the entire application's Tailwind and theme styles. It proves scoped component behavior, not complete application layout fidelity or measured rendering speed. No backend, database, provider, authorization, tenant or production deployment changes occurred.

Subsequent automated review found two material issues. At a 601px viewport and a 521px toolbar width, the new controls overlapped; the reviewer reproduced this using built global CSS and actual components. The toolbar now stacks at 820px while unrelated 600px rules remain unchanged. The existing browser case reproduced the failure before the CSS fix and now checks control rectangles without a build-output dependency. It supplies the constrained width, box sizing and input padding explicitly. The shared dark foreground also used a cutoff calibrated for black while emitting brown. On #767676, brown gave 3.76:1 contrast; black gives 4.62:1. The helper now emits black and the existing color table includes that counterexample. A fresh reviewer for this PR independently passed the fourteen touched-suite cases and approved source `7f0e77d17b3c160a075107d959041c3f3098ea79`. No additional test case or framework was added for these corrections.
